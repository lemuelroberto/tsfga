name: ${PREFIX:-tsfga}
networks:
  default:
    external: false
    name: ${PREFIX:-tsfga}
services:
  postgres:
    command: ["postgres", "-c", "config_file=/etc/postgresql/postgresql.conf"]
    container_name: ${PREFIX:-tsfga}-postgres
    environment:
      PGDATA: "/var/lib/postgresql/18/docker"
      PGHOST: "/var/run/postgresql"
      PGUSER: "${POSTGRES_USER:-dev}"
      POSTGRES_DB: "${POSTGRES_DB:-dev}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-password}"
      POSTGRES_USER: "${POSTGRES_USER:-dev}"
      TZ: "America/Sao_Paulo"
    healthcheck:
      interval: 10s
      retries: 5
      start_period: 30s
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      timeout: 10s
    image: postgres:18-alpine@sha256:9a8afca54e7861fd90fab5fdf4c42477a6b1cb7d293595148e674e0a3181de15
    ports:
      - ${POSTGRES_PORT:-5432}:5432
    restart: unless-stopped
    volumes:
      - postgres-data:/var/lib/postgresql/18/docker
      - ./infra/postgresql/postgresql.conf:/etc/postgresql/postgresql.conf:ro
      - ./infra/postgresql/init-openfga-schema.sql:/docker-entrypoint-initdb.d/init-openfga-schema.sql:ro

  openfga-migrate:
    depends_on:
      postgres:
        condition: service_healthy
    image: openfga/openfga:v1.18.2@sha256:4a5e32241c6900a46c32ddb9fce461e543b40f2dcbdcabbb59e28b72e7a18647
    container_name: ${PREFIX:-tsfga}-openfga-migrate
    environment:
      - OPENFGA_DATASTORE_ENGINE=postgres
      - OPENFGA_DATASTORE_URI=postgres://${POSTGRES_USER:-dev}:${POSTGRES_PASSWORD:-password}@postgres:5432/${POSTGRES_DB:-dev}?sslmode=disable&search_path=openfga
    command: migrate
    networks:
      - default

  openfga:
    depends_on:
      openfga-migrate:
        condition: service_completed_successfully
    image: openfga/openfga:v1.18.2@sha256:4a5e32241c6900a46c32ddb9fce461e543b40f2dcbdcabbb59e28b72e7a18647
    container_name: ${PREFIX:-tsfga}-openfga
    command: run
    environment:
      - OPENFGA_DATASTORE_ENGINE=postgres
      - OPENFGA_DATASTORE_URI=postgres://${POSTGRES_USER:-dev}:${POSTGRES_PASSWORD:-password}@postgres:5432/${POSTGRES_DB:-dev}?sslmode=disable&search_path=openfga
      # Deliberately well below the server's max_connections, so a
      # warm OpenFGA pool cannot refuse everyone else a connection.
      # See infra/postgresql/postgresql.conf.
      - OPENFGA_DATASTORE_MAX_OPEN_CONNS=50
      - OPENFGA_PLAYGROUND_ENABLED=true
    networks:
      - default
    ports:
      - ${FGA_HTTP_PORT:-8080}:8080 #http
      - ${FGA_GRPC_PORT:-8081}:8081 #grpc
      - ${FGA_PLAYGROUND_PORT:-3000}:3000 #playground
      - ${FGA_METRICS_PORT:-2112}:2112 #prometheus metrics
    healthcheck:
      test:
        [
          "CMD",
          "/usr/local/bin/grpc_health_probe",
          "-addr=openfga:8081"
        ]
      interval: 5s
      timeout: 30s
      retries: 3

volumes:
  postgres-data:
    driver: local
    name: ${PREFIX:-tsfga}-postgres-data
